# Server Properties
server:
  port: 8080

# Spring Properties
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/bookstore
    username: root
    password: Test#12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  profiles:
    active: dev